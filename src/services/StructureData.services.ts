export const structureDataArray = async (data: any[]) => {
console.log("🚀 ~ file: StructureData.services.ts:2 ~ structureDataArray ~ data:", data[0])
const {
    dataValues
}=data[0]

    return dataValues
}